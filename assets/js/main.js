const h=document.querySelector('.header');addEventListener('scroll',()=>h?.classList.toggle('fixed',scrollY>80));const o=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>o.observe(e));const m=document.querySelector('.mobile');document.querySelectorAll('[data-menu]').forEach(b=>b.onclick=()=>m?.classList.toggle('open'));document.querySelectorAll('[data-demo-form]').forEach(f=>f.onsubmit=e=>{e.preventDefault();const x=f.querySelector('[data-msg]');if(x)x.textContent='Form ready for backend integration.'});